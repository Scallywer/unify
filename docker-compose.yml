services:
  health-dashboard:
    image: ghcr.io/scallywer/unify:latest
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/health.db
      - AUTH_SECRET=123456
    restart: unless-stopped
